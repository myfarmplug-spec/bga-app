{
  "buildCommand": "cd client/bga-ui && npm install && npm run build",
  "outputDirectory": "client/bga-ui/dist",
  "installCommand": "npm install",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
